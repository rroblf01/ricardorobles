---
import "../styles/global.css";

export interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const {
  title = "Ricardo Robles — FullStack Developer",
  description = "Portafolio de Ricardo Robles. Programador backend con 5+ años de experiencia en Python, Django, Docker y TypeScript.",
  ogImage = "https://ricardorobles.es/assets/meta-portafolios.jpeg",
} = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      html { background-color: #E6E7C6; }
      html.dark { background-color: #111827; }
    </style>
    <script is:inline>
      (function() {
        const stored = localStorage.getItem("theme");
        const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
        if (stored === "dark" || (!stored && prefersDark)) {
          document.documentElement.classList.add("dark");
        }
      })();
    </script>
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href="https://ricardorobles.es/" />

    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:url" content="https://ricardorobles.es/" />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="es_ES" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Ricardo Robles",
        "jobTitle": "FullStack Developer",
        "url": "https://ricardorobles.es",
        "sameAs": [
          "https://linkedin.com/in/ricardoroblesfernandez/",
          "https://github.com/rroblf01"
        ],
        "knowsAbout": ["Python", "Django", "FastAPI", "Docker", "PostgreSQL", "TypeScript", "JavaScript", "Git"],
        "description": "Programador FullStack con fuerte predilección al Backend. Ingeniero Informático con experiencia en Python, Django, Docker y TypeScript."
      }
    </script>

    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://region1.google-analytics.com" />
    <link rel="preconnect" href="https://www.googletagmanager.com" />
    <link rel="preload" href="/assets/ricardo.webp" as="image" />

    <script async src="https://www.googletagmanager.com/gtag/js?id=G-31TBY36R16"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-31TBY36R16');
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
